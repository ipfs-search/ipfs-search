---
- name: Install Golang, GIT
  apt:
    pkg:
      - golang-go
      - git
    state: installed
  become: true
- block:
  - file: path="/home/vagrant/gocode" state=directory user=vagrant group=vagrant
  - command: go get .
    args:
      chdir: /vagrant
