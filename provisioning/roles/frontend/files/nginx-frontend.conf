proxy_cache_path /etc/nginx/cache levels=1:2 keys_zone=search_cache:64m max_size=10g
             inactive=8h use_temp_path=off;

limit_req_zone $binary_remote_addr zone=search_reqs:10m rate=10r/s;
limit_conn_zone $binary_remote_addr zone=search_conns:10m;

server {
    listen 80 default_server;
    listen [::]:80 default_server;

    server_name _;
    rewrite ^/(.*) http://ipfs-search.com/$1 permanent;
}

server {
    listen 80;
    listen [::]:80;

    server_name ipfs-search.com;

    root /usr/local/libexec/ipfs-search-frontend;

    index index.html;

    # Redirect API to different host
    rewrite ^/v1(.*) http://api.ipfs-search.com/v1$1 permanent;
    rewrite ^/api(.*) http://api.ipfs-search.com$1 permanent;

    location / {
        # First attempt to serve request as file, then
        # as directory, then fall back to displaying a 404.
        try_files $uri $uri/ =404;
    }

    error_page 404 /404.html;
    error_page 500 502 503 504 /500.html;
}

server {
    listen 80;
    listen [::]:80;

    server_name api.ipfs-search.com;

    limit_req zone=search_reqs burst=50;
    limit_conn search_conns 200;

    location /v1/search {
        proxy_pass http://youngone.ipfs-search.com/v1/search;

        proxy_cache search_cache;
        proxy_cache_use_stale updating;
        proxy_cache_lock on;
        proxy_cache_min_uses 1;
        proxy_cache_valid 200 8h;
        proxy_cache_valid any 0;

        add_header X-Cache-Status $upstream_cache_status;
        add_header Access-Control-Allow-Origin '*';
    }
}
